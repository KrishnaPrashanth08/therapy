[phases]
setup = "corepack enable && corepack prepare npm@latest --activate"

[build]
command = "cd therapy-app && npm install && npm run build && cd ../backend && npm install"

[start]
command = "node backend/server.js"

[variables]
NODE_VERSION = "18"
