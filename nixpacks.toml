[phases]
  [phases.setup]
  command = "corepack enable && corepack prepare npm@latest --activate"

[build]
  command = "npm install --prefix therapy-app && npm run build --prefix therapy-app && cd ../backend && npm install"

[start]
  command = "node backend/server.js"

[variables]
  NODE_VERSION = "18"
